---
- name: InStall and Start NGINX
  hosts: all
  become: yes
  tasks:
    - name: Install NGINX
      yum:
        name: nginx
        state: present
    - name: Start NGINX service
      service:
        name: nginx
        state: started
        enabled: true
